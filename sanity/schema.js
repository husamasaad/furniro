import gallary from './schemas/gallary'
import heroInfo from './schemas/heroInfo'
import products from './schemas/products'
import users from './schemas/users'

export const schema = {
  types: [heroInfo, products, users, gallary],
}
